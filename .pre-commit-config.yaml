repos:
  - repo: local
    hooks:
      - id: dprint
        name: dprint format
        entry: uv run dprint fmt --allow-no-files
        language: system
        types: [text]
        require_serial: true
